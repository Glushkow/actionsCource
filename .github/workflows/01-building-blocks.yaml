name: 01-Building-blocks

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello, world!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Success steps
              run: |
                echo "Goodbye, world!"
                exit 0
            - name: Say goodbye
              run: echo "Goodbye, world!"
